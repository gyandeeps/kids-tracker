import type { NextPage } from "next";
import Head from "next/head";

const App: NextPage = () => {
    return (
        <>
            <Head>
                <title>App</title>
            </Head>
            <div>App</div>
        </>
    );
};

export default App;
